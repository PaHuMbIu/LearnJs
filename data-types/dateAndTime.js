/**
 * Объект Date в JavaScript предназначен для работы с датой и временем.
 * Он предоставляет разнообразные методы для создания, получения и изменения информации о времени
 */

// 📅 Создание объекта Date
// Существует несколько способов создать объект Date:

// Текущая дата и время:
const now = new Date();
console.log(now); // Текущие дата и время

// По таймстампу (миллисекунды с 1 января 1970 UTC):
let date1 = new Date(0); // 01.01.1970 UTC
let datePlusOneDay = new Date(24 * 3600 * 1000); // 02.01.1970 UTC

// Из строки:
const date2 = new Date("2025-05-23");
console.log(date2);

// С указанием компонентов:
const date3 = new Date(2025, 4, 23, 10, 30); // 23 мая 2025, 10:30
console.log(date3);

/**
 * месяцы нумеруются с 0 (январь) до 11 (декабрь).
 */

// 🕒 Получение компонентов даты
/**
 * Методы для получения различных частей даты:
 *
 * getFullYear() — год (например, 2025)
 *
 * getMonth() — месяц от 0 до 11
 *
 * getDate() — день месяца от 1 до 31
 *
 * getDay() — день недели от 0 (воскресенье) до 6 (суббота)
 *
 * getHours(), getMinutes(), getSeconds(), getMilliseconds()
 *
 * Для получения времени в формате UTC используйте методы с приставкой getUTC*()
 */

// ✍️ Установка компонентов даты
/**
 * Методы для изменения компонентов даты:
 *
 * setFullYear(year, [month], [date])
 *
 * setMonth(month, [date])
 *
 * setDate(date)
 *
 * setHours(hours, [minutes], [seconds], [ms])
 *
 * setMinutes(minutes, [seconds], [ms])
 *
 * setSeconds(seconds, [ms])
 *
 * setMilliseconds(ms)
 */
// Пример:
const date4 = new Date();
date4.setFullYear(2025);
date4.setMonth(4); // Май
date4.setDate(23);
console.log(date4);

// 🧮 Преобразование и форматирование
/**
 * Методы для преобразования даты в различные форматы:
 *
 * getTime() — возвращает таймстамп (количество миллисекунд с 1 января 1970 UTC)
 *
 * toDateString() — строка с датой (например, "Fri May 23 2025")
 *
 * toTimeString() — строка с временем (например, "10:30:00 GMT+0200")
 *
 * toISOString() — ISO 8601 формат (например, "2025-05-23T08:30:00.000Z")
 *
 * toLocaleString() — строка с датой и временем в локальном формате
 */
// Пример:
const date5 = new Date();
console.log(date5.toDateString()); // "Fri May 23 2025"
console.log(date5.toTimeString()); // "10:30:00 GMT+0200"
console.log(date5.toISOString());  // "2025-05-23T08:30:00.000Z"

// 🧠 Дополнительные возможности
/**
 * Парсинг строки в дату: Date.parse("2025-05-23") возвращает таймстамп.
 *
 * Автоматическая корректировка:
 * Если установить значение, выходящее за пределы допустимого, объект Date автоматически скорректирует дату.
 *
 * Сравнение дат: Объекты Date можно сравнивать с помощью операторов сравнения.
 */

const date6 = new Date(2025, 4, 33); // 2 июня 2025
console.log(date6);

const date7 = new Date(2025, 4, 23);
const date8 = new Date(2025, 4, 24);
console.log(date7 < date8); // true

//====================================================================================================================//
/**
 * Tasks
 */
//--------------------------------------------------------------------------------------------------------------------//
// Создайте дату

/**
 * Создайте объект Date для даты: 20 февраля 2012 года, 3 часа 12 минут. Временная зона – местная.
 */

// console.log(new Date(2012, 1, 20, 3, 12))
//====================================================================================================================//
// Покажите день недели

/**
 * Напишите функцию getWeekDay(date), показывающую день недели в коротком формате: «ПН», «ВТ», «СР», «ЧТ», «ПТ», «СБ», «ВС».
 */

// const date = new Date(2012, 0, 3);

// const getWeekDay = date => {
//     const days = ['ВС', 'ПН', 'ВТ', 'СР', 'ЧТ', 'ПТ', 'СБ'];
//
//     return days[date.getDay()];
// };
// console.log(getWeekDay(date)); // "ВТ"

//====================================================================================================================//
// День недели в европейской нумерации

/**
 * В Европейских странах неделя начинается с понедельника (день номер 1),
 * затем идёт вторник (номер 2) и так до воскресенья (номер 7).
 * Напишите функцию getLocalDay(date), которая возвращает «европейский» день недели для даты date.
 */

// const date = new Date(2012, 0, 3) // Вторник

// const getLocalDay = date => {
//     let day = date.getDay();
//
//     if (day === 0) day = 7;
//
//     return day;
// };
// console.log(getLocalDay(date)); // 2

//====================================================================================================================//
// Какой день месяца был много дней назад?

/**
 * Создайте функцию getDateAgo(date, days), возвращающую число, которое было days дней назад от даты date.
 *
 * К примеру, если сегодня двадцатое число, то getDateAgo(new Date(), 1)
 * вернёт девятнадцатое и getDateAgo(new Date(), 2) – восемнадцатое.
 *
 * Функция должна надёжно работать при значении days=365 и больших значениях:
 */

// const date = new Date(2015, 0, 2);
//
// const getDateAgo = (date, days) => {
//
// };
// console.log(getDateAgo(date, 1)); // 1, (1 Jan 2015)
// console.log(getDateAgo(date, 2)); // 31, (31 Dec 2014)
// console.log(getDateAgo(date, 365)); // 2, (2 Jan 2014)



