/**
 * JSON — это текстовый формат, который используется для хранения и обмена данными между программами,
 * особенно в интернете (например, между клиентом и сервером).
 *
 * Он очень похож на JavaScript-объекты, но есть некоторые ограничения и отличия.
 */

// 🧱 Пример JSON
/**
 * {
 *     "name": "Иван",
 *     "age": 25,
 *     "isStudent": false
 * }
 */

// 🔁 Преобразование между JSON и JavaScript

/**
 * В JavaScript есть 2 главные функции для работы с JSON:
 *
 * Функция	            Что делает
 * JSON.stringify(obj)	Преобразует JavaScript-объект в JSON-строку
 * JSON.parse(json)	    Преобразует JSON-строку в JavaScript-объект
 */

// 🔷 Пример JSON.stringify
let user = {
    name: "Иван",
    age: 25
};
//
let json = JSON.stringify(user);
console.log(json); // '{"name":"Иван","age":25}'

// 🔶 Пример JSON.parse
let json = '{"name":"Иван","age":25}';
let user = JSON.parse(json);
//
console.log(user.name); // Иван

// ⚠️ Ограничения JSON

/**
 * Некоторые вещи в JSON нельзя использовать, хотя они есть в JavaScript:
 *
 * Нельзя в JSON	    Почему
 * Функции	JSON        хранит только данные, а не поведение
 * undefined	        Не поддерживается
 * Символы (Symbol)	    Не поддерживаются
 * Комментарии	        Нельзя (в отличие от кода JS)
 */

// ✅ Что можно в JSON:

/**
 * Тип	            Пример
 * Объекты	        { "key": "value" }
 * Массивы	        [1, 2, 3]
 * Строки	        "text"
 * Числа	        123
 * true / false	    логические значения
 * null	            специальное значение
 */

// 📌 Почему JSON — это важно?

/**
 * Потому что большинство серверов в интернете отправляют данные в виде JSON.
 * Например, сайт отправляет запрос на сервер, а сервер возвращает ответ:
 */

/**
 * {
 *     "products": [
 *     { "id": 1, "name": "Телефон" },
 *     { "id": 2, "name": "Ноутбук" }
 *     ]
 * }
 */


