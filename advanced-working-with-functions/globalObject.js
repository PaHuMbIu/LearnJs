/**
 * Глобальный объект предоставляет переменные и функции, доступные в любом месте программы.
 * По умолчанию это те, что встроены в язык или среду исполнения. В браузере он называется window,
 * в Node.js — global, в другой среде исполнения может называться иначе.
 */

// 🔑 Основные свойства и поведение

/**
 * Глобальные переменные и функции:
 * Переменные, объявленные с помощью var, и функции, объявленные на верхнем уровне,
 * становятся свойствами глобального объекта.
 */

var x = 10;
function greet() {
    console.log("Hello!");
}
console.log(window.x); // 10
window.greet();        // "Hello!"

/**
 * let и const: Переменные, объявленные с помощью let или const, не становятся свойствами глобального объекта
 */

let y = 20;
console.log(window.y); // undefined

/**
 * window — это глобальный объект браузера, который предоставляет доступ к:
 * глобальным переменным/функциям,
 * DOM-дереву,
 * API браузера (таймеры, location, alert и т.д.),
 * и прочим компонентам веб-окружения.
 */