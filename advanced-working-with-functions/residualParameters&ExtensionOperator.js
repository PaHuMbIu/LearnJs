/**
 * Когда вы пишете функцию, но не знаете заранее, сколько аргументов в неё придёт — используйте ...
 * (три точки) перед именем параметра. Это называется rest-параметром (от англ. "rest" — "остаток").
 */

// 📦 1. Оператор Rest: собираем все аргументы
function showNames(...names) {
  console.log(names); // массив всех переданных аргументов
}

showNames("Аня", "Игорь", "Вася");

// ["Аня", "Игорь", "Вася"]

/**
 * Важно:
 * ... может быть только один
 * Он должен быть последним в списке параметров
 */

// 🧮 2. Остальные параметры функции

function showInfo(first, second, ...others) {
  console.log(first); // первый аргумент
  console.log(second); // второй аргумент
  console.log(others); // все остальные аргументы
}

showInfo("яблоко", "груша", "банан", "киви");
// "яблоко"
// "груша"
// ["банан", "киви"]

// 📤 3. Оператор Spread: передаём массив как отдельные значения

/**
 * Spread — это тоже ..., но работает наоборот: раскладывает массив на отдельные элементы.
 */

let fruits = ["яблоко", "банан", "груша"];
console.log(...fruits);
// Выведет: "яблоко" "банан" "груша"

Math.max(...[3, 5, 1]);
// то же самое, что Math.max(3, 5, 1)

// 🧩 4. Копирование и объединение массивов через spread

let arr = [1, 2, 3];
let copy = [...arr]; // новый массив

// Объединение

let arr1 = [1, 2];
let arr2 = [3, 4];
let merged = [...arr1, ...arr2]; // [1, 2, 3, 4]

// 📚 5. Spread работает и с объектами (ES2018)

// Можно так же "развернуть" объект:
let user = {name: "Вася", age: 30};
let clone = {...user}; // копия объекта
console.log(clone);

// И объединить несколько объектов:

let a = {name: "Вася"};
let b = {age: 30};
let merged2 = {...a, ...b}; // { name: "Вася", age: 30 }
console.log(merged2);

// Если есть одинаковые ключи — значение последнего перезапишет остальные:

let merged3 = {...a, name: "Петя"};
console.log(merged3); // name будет "Петя"
