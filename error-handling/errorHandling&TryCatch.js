/**
 * Конструкция try..catch в JavaScript используется для обработки ошибок во время выполнения кода.
 * Она позволяет избежать немедленного завершения работы программы при возникновении ошибки,
 * предоставляя возможность обработать её и продолжить выполнение.
 */

// 🔧 Основы конструкции try..catch
try {
  // Код, который может вызвать ошибку
} catch (err) {
  // Код обработки ошибки
}

// 🧠 Пример без ошибок
try {
  console.log("Начало блока try");
  // Код без ошибок
  console.log("Конец блока try");
} catch (err) {
  console.log("Блок catch не выполнится");
}

// ⚠️ Пример с ошибкой
try {
  console.log("Начало блока try");
  nonExistentFunction(); // Ошибка: функция не определена
  console.log("Конец блока try");
} catch (err) {
  console.log("Ошибка поймана:", err.message);
}

/**
 * try...catch работает синхронно,
 * то есть он не перехватывает ошибки, возникающие в асинхронных операциях, таких как setTimeout
 */

try {
  setTimeout(function() {
    noSuchVariable; // скрипт упадёт тут
  }, 1000);
} catch (e) {
  console.log("не сработает");
}
