/**
 * 🧠 Что такое WeakMap и WeakSet?
 *
 * Это специальные коллекции, похожие на Map и Set, но с двумя ключевыми отличиями:
 *
 * 1) Ключи и значения: В WeakMap ключами могут быть только объекты,
 * а в WeakSet хранятся только объекты.
 *
 * 2) Слабые ссылки: Эти структуры не препятствуют удалению объектов сборщиком мусора,
 * если на них больше нет других ссылок.
 * Это помогает избежать утечек памяти.
 */

// 🔐 WeakMap — хранение данных, связанных с объектами
/**
 * WeakMap позволяет ассоциировать данные с объектами без риска утечки памяти. Пример:
 */

// const weakMap = new WeakMap();
//
// let user = { name: "Alice" };
// weakMap.set(user, "Приватные данные");
//
// console.log(weakMap.get(user)); // "Приватные данные"
//
// user = null; // Объект больше не доступен, его можно удалить из памяти

// ✅ WeakSet — отслеживание состояния объектов
/**
 * WeakSet используется для хранения уникальных объектов. Пример:
 */

// const messages = [
//     { text: "Hello", from: "John" },
//     { text: "How goes?", from: "John" },
//     { text: "See you soon", from: "Alice" }
// ];
//
// let readMessages = new WeakSet();
//
// // Отметим первое сообщение как прочитанное
// readMessages.add(messages[0]);
//
// console.log(readMessages.has(messages[0])); // true
//
// // Если сообщение удаляется из массива, оно также удаляется из WeakSet
// messages.shift();

// ⚠️ Ограничения
/**
 * Неитерируемость: Нельзя перебрать элементы WeakMap или WeakSet.
 *
 * Нет свойства size: Нельзя узнать количество элементов.
 *
 * Только объекты: Нельзя использовать примитивы (например, строки или числа) в качестве ключей или значений.
 */

// 🛠️ Когда использовать?
/**
 * Хранение приватных данных: Например, в библиотеках для хранения внутренних данных объектов без их изменения.
 *
 * Отслеживание состояния объектов: Например, для отметки прочитанных сообщений или активных элементов DOM.
 *
 * Управление памятью: В случаях, когда важно, чтобы данные автоматически удалялись при удалении объектов.
 */

//====================================================================================================================//
/**
 * Tasks
 */
//--------------------------------------------------------------------------------------------------------------------//
