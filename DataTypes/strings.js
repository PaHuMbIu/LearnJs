// ====== Что такое строка ======

/**
 * Строка — это просто текст.
 * Можно использовать разные виды кавычек: '', "", ``
 */

// let str1 = "Привет";
// let str2 = 'Мир';
// let str3 = `Как дела?`;

// ====== Шаблонные строки (обратные кавычки) ======

/**
 * С помощью обратных кавычек можно вставлять переменные прямо в строку
 */

// let name = "Бро";
// let greeting = `Привет, ${name}!`; // Привет, Бро!

// console.log(greeting);

// ====== Доступ к символам строки ======

/**
 * Строка — это как массив символов, но менять их нельзя
 */

// let str = "JavaScript";

// console.log(str[0]);         // J
// console.log(str.charAt(1));  // a

// str[0] = 'X'; ❌ Нельзя менять символы напрямую

// ====== Длина строки ======

/**
 * .length возвращает количество символов
 */

// console.log(str.length); // 10

// ====== Перебор строки ======

/**
 * Можно перебрать строку посимвольно
 */

// for (let char of "Мир") {
//     console.log(char);
// М
// и
// р
// }

// ====== Поиск в строке ======

/**
 * indexOf — вернёт индекс (или -1 если не найдено)
 * includes — true/false
 * startsWith — начинается ли строка с...
 * endsWith — заканчивается ли строка на...
 */

// let text = "Брат по братски";

// console.log(text.indexOf("по"));      // 5
// console.log(text.includes("брат"));   // true
// console.log(text.startsWith("Брат")); // true
// console.log(text.endsWith("ски"));    // true

// ====== Вырезание подстроки ======

/**
 * slice(start, end) — вырезает с позиции start до end (не включая end)
 * substring(start, end) — почти то же самое, но работает по-другому с отрицательными значениями
 * substr(start, length) — устаревший способ, но иногда встречается
 */

// let example = "JavaScript";

// console.log(example.slice(0, 4));     // Java
// console.log(example.substring(4, 10)); // Script
// console.log(example.substr(4, 6));     // Script (устарело)

// ====== Замена текста ======

/**
 * replace — заменяет первое вхождение
 * replaceAll — заменяет все вхождения (поддерживается не везде)
 */

// let phrase = "Я брат, ты брат, мы братья";
// console.log(phrase.replace("брат", "кореш"));    // Я кореш, ты брат, мы братья
// console.log(phrase.replaceAll("брат", "кореш")); // Я кореш, ты кореш, мы братья

// ====== Изменение регистра ======

/**
 * toUpperCase — в верхний регистр
 * toLowerCase — в нижний
 */

// let shout = "спокойно";
// console.log(shout.toUpperCase()); // СПОКОЙНО

// let loud = "ГРОМКО";
// console.log(loud.toLowerCase());  // громко

// ====== Юникод и суррогатные пары ======

/**
 * Некоторые символы (эмодзи, 🇷🇺 и т.п.) занимают 2 позиции
 */

// let emoji = "😎";
// console.log(emoji.length); // 2

// ====== ИТОГ: самые частые приёмы ======

/**
 * str[0] — достать символ
 * str.includes("текст") — проверить наличие
 * str.slice(...) — вырезать
 * str.replace(...) — заменить
 * str.toLowerCase() — регистр
 */

// let msg = "  Привет, брат!  ";
// let clean = msg.trim().toUpperCase(); // Пример как очистить и изменить регистр

// console.log(clean); // ПРИВЕТ, БРАТ!

//====================================================================================================================//
/**
 * Tasks
 */
//--------------------------------------------------------------------------------------------------------------------//
// Сделать первый символ заглавным

/**
 * Напишите функцию ucFirst(str), возвращающую строку str с заглавным первым символом. Например:
 *
 * ucFirst("вася") == "Вася";
 */

// const ucFirst = str => {
//     return str.at(0).toUpperCase() + str.slice(1);
// };
// console.log(ucFirst("вася")); // Вася

//====================================================================================================================//
// Проверка на спам

/**
 * Напишите функцию checkSpam(str), возвращающую true, если str содержит 'viagra' или 'XXX', а иначе false.
 *
 * Функция должна быть нечувствительна к регистру
 */

// const checkSpam = str => {
//     return str.toLowerCase().includes("viagra") || str.toLowerCase().includes("xxxxx");
// };
// console.log(checkSpam('buy ViAgRA now')) // true
// console.log(checkSpam('free xxxxx')) // true
// console.log(checkSpam("innocent rabbit")) // false

//====================================================================================================================//
// Усечение строки

/**
 * Создайте функцию truncate(str, maxlength), которая проверяет длину строки str и,
 * если она превосходит maxlength, заменяет конец str на "…", так, чтобы её длина стала равна maxlength.
 *
 * Результатом функции должна быть та же строка, если усечение не требуется, либо, если необходимо, усечённая строка.
 */

// const truncate = (str, maxLength) => {
//     return str.length > maxLength  ? str.slice(0, 19) + "..." : str;
// };
// console.log(truncate("Вот, что мне хотелось бы сказать на эту тему:", 20)); // "Вот, что мне хотело…"
// console.log(truncate("Всем привет!", 20)); // "Всем привет!"
