/**
 * ✅ В JavaScript один тип чисел: number
 * Он используется и для целых, и для дробных значений.
 */

// let integer = 10;
// let float = 3.14;

/**
 * ⚠️ Особенности чисел:
 * - NaN — "Not a Number", возникает при ошибке в вычислении
 * - Infinity и -Infinity — бесконечности
 * - Погрешности при работе с дробями
 */

// console.log("abc" / 2);       // NaN
// console.log(1 / 0);           // Infinity
// console.log(0.1 + 0.2);       // 0.30000000000000004 ❌

/**
 * 💡 Правильное сравнение дробей:
 */
// let sum = 0.1 + 0.2;
// console.log(Math.abs(sum - 0.3) < 0.000001); // true ✅

/**
 * 🔍 Проверка значений:
 */
// console.log(isNaN("abc" / 2));      // true
// console.log(isFinite("15"));        // true
// console.log(isFinite("abc"));       // false

/**
 * 🔢 Преобразование строк в числа:
 */
// console.log(parseInt("100px"));     // 100
// console.log(parseFloat("12.5em"));  // 12.5

/**
 * 🧮 parseInt с системой счисления:
 */
// console.log(parseInt("ff", 16));    // 255

/**
 * 🧾 Округление:
 */
// console.log(Math.floor(3.7));       // 3 — вниз
// console.log(Math.ceil(3.2));        // 4 — вверх
// console.log(Math.round(3.5));       // 4 — до ближайшего
// console.log(Math.trunc(3.9));       // 3 — просто отбрасывает дробную часть

/**
 * 📌 toFixed(n) — округляет до n знаков после запятой, но возвращает СТРОКУ
 */
// let n = 1.23456;
// console.log(n.toFixed(2));          // "1.23" (строка)
// console.log(+n.toFixed(2));         // 1.23 (число)

/**
 * ✅ Главное:
 * - В JS все числа — тип number
 * - Будь осторожен с дробями (погрешности)
 * - Используй isNaN, isFinite для проверок
 * - Используй parseInt, parseFloat для строк
 * - toFixed — для округления до знаков после запятой
 */

//====================================================================================================================//
/**
 * Tasks
 */
//--------------------------------------------------------------------------------------------------------------------//
// Ввод числового значения

/**
 * Создайте функцию readNumber, которая будет запрашивать ввод числового значения до тех пор, пока посетитель его не введёт.
 *
 * Функция должна возвращать числовое значение.
 *
 * Также надо разрешить пользователю остановить процесс ввода, отправив пустую строку или нажав «Отмена».
 * В этом случае функция должна вернуть null.
 */

// const readNumber = () => {
//     let num = NaN;
//
//     while (true) {
//         if (!isNaN(num) && typeof num === "number") {
//             return num;
//         } else {
//             num = +prompt("Введите число", 0);
//         }
//     }
// };
// const result = readNumber();
// console.log(`Число: ${ result || result === 0 ? result : null}`);

// let i = 0;
// while (i != 10) {
//     console.log(i += 0.2);
//     i += 0.2;
// }
//====================================================================================================================//
// Случайное число от min до max

/**
 * Встроенный метод Math.random() возвращает случайное число от 0 (включительно) до 1 (но не включая 1)
 *
 * Напишите функцию random(min, max), которая генерирует случайное число с плавающей точкой от min
 * до max (но не включая max).
 */

// const random = (min, max) => {
//     return Math.floor(min + Math.random() * (max - min));
// };
// console.log(random(1, 5));
// console.log(random(1, 6));
// console.log(random(1, 10));