/**
 * 🔹 Примитивы — это простые типы данных:
 * string, number, boolean, null, undefined, symbol, bigint
 */

// let str = "Привет";
// let num = 42;
// let flag = true;

/**
 * 🔹 Хотя примитивы не объекты, у них можно вызывать методы.
 * JavaScript автоматически превращает примитив во временный объект.
 */

// console.log(str.toUpperCase()); // "ПРИВЕТ"

/**
 * ❓ Что происходит при вызове метода у примитива:
 * 1. Примитив временно оборачивается в объект-обёртку
 * 2. Вызывается нужный метод
 * 3. Объект удаляется
 *
 * Пример:
 * "привет".toUpperCase() → new String("привет").toUpperCase()
 */

/**
 * Объекты-обёртки:
 * string  → String
 * number  → Number
 * boolean → Boolean
 */

/**
 * ⚠️ Важно:
 * Эти обёртки временные, поэтому нельзя сохранять в них свойства.
 */

// let hello = "Hello";
// hello.test = 123; // Вызываешь у примитива test как метод, который создаётся и тут же удаляется
// console.log(hello.test); // undefined — значение не сохраняется

/**
 * 🔹 Вывод:
 * - Примитивы автоматически оборачиваются во временные объекты
 * - Это называется автооборачиванием
 * - Добавлять свойства в примитивы нельзя — они "исчезают"
 */

/** ✅ Примеры полезных методов примитивов: */

// Строки:
// console.log(" test ".trim());        // "test"
// console.log("abc".includes("b"));    // true
//
// // Числа:
// let n = 123.456;
// console.log(n.toFixed(1));           // "123.5"
//
// // Булевы значения:
// let bool = true;
// console.log(bool.toString());        // "true"
